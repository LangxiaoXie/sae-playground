# pip install -r requirements.txt  (or: uv pip install -r requirements.txt)
torch
numpy
scikit-learn
plotly
transformer-lens     # hooks into GPT-2 to read its residual stream
sae-lens             # only for the pretrained reference SAE in notebook 2
datasets             # pulls the text corpus for building the activation cache

# running the notebooks
ipykernel
nbformat

# tests
pytest
